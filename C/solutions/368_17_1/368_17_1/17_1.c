#include <stdio.h>

void MaxAndMin(int * arr2, int * maxPtr2, int * minPtr2);

int main(void)
{
	int * maxPtr;
	int * minPtr;
	int arr[5];
	int i;

	for (i = 0; i < 5; i++)
	{
		printf("정수를 입력하세요: ");
		scanf("%d", &arr[i]);
	}


}

void MaxAndMin(int * arr2, int * maxPtr2, int * minPtr2)
{

}